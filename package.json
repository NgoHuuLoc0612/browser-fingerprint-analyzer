{
  "name": "browser-fingerprint-analyzer",
  "version": "2.0.0",
  "private": true,
  "description": "Production-grade browser fingerprinting with entropy clustering, anomaly detection, probabilistic identity matching, WebGL2/WebGPU/WASM SIMD.",
  "scripts": {
    "install:all": "npm install --prefix frontend --legacy-peer-deps && npm install --prefix backend --legacy-peer-deps",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build:frontend": "npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build:wasm": "cd wasm-simd && bash build_wasm.sh",
    "build": "npm run build:backend && npm run build:frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
